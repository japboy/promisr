{
  "name": "promisr",
  "version": "0.1.0",
  "description": "Promise utils",
  "main": "./dist/promisr.js",
  "scripts": {
    "test": "npm run eslint && npm run mocha",
    "eslint": "eslint ./index.js",
    "mocha": "mocha --compilers js:babel-register ./test/index.js",
    "testem": "testem ci",
    "browserify": "browserify ./index.js -o ./dist/promisr.js -t babelify -t browserify-shim -s Promisr --no-bundle-external",
    "uglify": "uglifyjs ./dist/promisr.js -o ./dist/promisr.min.js",
    "dist": "npm run browserify && npm run uglify",
    "doc": "node ./scripts/make-readme.js",
    "release-patch": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version patch && git push origin master --tags && npm publish --access=public",
    "release-minor": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version minor && git push origin master --tags && npm publish --access=public",
    "release-major": "git checkout master && npm run dist; git commit -a -m 'Build dist/'; npm version major && git push origin master --tags && npm publish --access=public",
    "release": "npm run release-patch"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/japboy/promisr.git"
  },
  "author": {
    "name": "Yu Inao",
    "email": "yu.inao@facebook.com"
  },
  "license": "Unlicense",
  "homepage": "https://github.com/japboy/promisr",
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "browserify-shim": "^3.8.11",
    "chai": "^3.4.1",
    "eslint": "^1.10.3",
    "markdox": "^0.1.10",
    "mocha": "2.3.4",
    "testem": "^0.9.11",
    "uglify-js": "^2.6.1"
  },
  "peerDependencies": {
    "underscore": "^1.8.3"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "browserify-shim": {
    "mocha": "global:mocha",
    "underscore": "_"
  }
}
